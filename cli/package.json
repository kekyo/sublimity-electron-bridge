{
  "name": "@sublimity-electron-bridge/cli",
  "version": "0.0.4",
  "description": "Electron bridge generator for automatic IPC setup",
  "repository": {
    "type": "git",
    "url": "https://github.com/kekyo/sublimity-electron-bridge.git"
  },
  "homepage": "https://github.com/kekyo/sublimity-electron-bridge#readme",
  "keywords": [
    "electron",
    "ipc",
    "bridge",
    "automatic",
    "cli"
  ],
  "main": "dist/index.js",
  "bin": {
    "seb": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rv --npm --cwd . && vite build",
    "dev": "rv --npm --cwd . && vite build --watch",
    "test": "rv --npm --cwd . && vitest run",
    "test:ui": "rv --npm --cwd . && vitest --ui",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "commander": "^11.1.0",
    "glob": "^10.3.10",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.10",
    "@vitest/ui": "^3.2.4",
    "typescript": "^5.8.3",
    "vite": "^7.0.2",
    "vitest": "^3.2.4"
  }
}