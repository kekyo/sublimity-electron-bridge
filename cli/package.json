{
  "name": "sublimity-electron-bridge-cli",
  "version": "0.15.4",
  "description": "Electron bridge generator for automatic IPC setup (CLI)",
  "keywords": [
    "electron",
    "ipc",
    "bridge",
    "automatic",
    "generator",
    "cli"
  ],
  "main": "dist/index.js",
  "bin": {
    "seb": "dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rv --npm . && npm i && tsc --noEmit && vite build",
    "test": "rv --npm . && npm i && tsc --noEmit && vitest run"
  },
  "peerDependencies": {
    "commander": ">=11.1.0",
    "sublimity-electron-bridge-core": "file:../core",
    "typescript": ">=5.0.0"
  },
  "devDependencies": {
    "@types/node": ">=24.0.10",
    "@vitest/ui": ">=3.2.4",
    "screw-up": ">=0.8.1",
    "typescript": ">=5.8.3",
    "vite": ">=7.0.2",
    "vitest": ">=3.2.4"
  }
}